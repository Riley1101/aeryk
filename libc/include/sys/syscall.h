#ifndef _SYS_SYSCALL_H
#define _SYS_SYSCALL_H 1

/**
 * @brief Kernel System call numbers for x86_64 architecture. For Libc.
 *
 * Syscall numbers (SYS_*) come from <abi/syscall.h>, the shared kernel/userland
 * ABI also used by the kernel's own `arch/x86_64/include/syscall.h`.
 */

#include <abi/syscall.h>

#endif // !_SYS_SYSCALL_H
