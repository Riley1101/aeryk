#include <stdbool.h>
#include <stdio.h>

/**
 * @brief Writes a character to stdout.
 * 
 * TODO! port the unistd first and then use write() instead of printf() to avoid recursion.
 * 
 * @param ic The character to write.
 * @return Returns the character written as an unsigned char cast to an int or EOF on error
 */
bool putchar(int ic) { 
 return ic; 
}
